version: '3.8'

services:
  bot:
    build: .
    container_name: roundrobin-duel-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}

